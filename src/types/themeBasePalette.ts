import Color = require("color");

type themeBasePalette = {
  fg: Color;
  grey: Color;
  red: Color;
  orange: Color;
  yellow: Color;
  green: Color;
  aqua: Color;
  blue: Color;
  purple: Color;
  magenta: Color;
  bg: Color;
};

export { themeBasePalette };
