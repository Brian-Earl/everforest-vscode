import Color = require("color");

type foregroundBasePalette = {
  fg: Color;
  red: Color;
  orange: Color;
  yellow: Color;
  green: Color;
  aqua: Color;
  blue: Color;
  purple: Color;
};

export { foregroundBasePalette };
